#include <bits/stdc++.h>
using namespace std;

int main() {
    string s, t;
    cin >> s >> t;
    int pos = 0;
    for (char c : t) {
        if (pos < s.size() && s[pos] == c) pos++;
    }
    cout << pos + 1 << "\n";
}
