#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    int hours = 0, rem = 0;

    while (a > 0) {
        hours += a;
        rem += a;
        a = rem / b;
        rem %= b;
    }

    cout << hours;
}
